import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'KineAppAdmin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
